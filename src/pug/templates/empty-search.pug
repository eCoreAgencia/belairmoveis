extends ../layouts/default.pug

block vars
  - var bodyClass = 'catalog category'
  - var pageTitle = 'empty-search search'

block head
  //- conteúdo que será inserido dentro do `head` da página

block content
  .container
    .empty-search__content
      span.empty-search__title Você buscou por:
      span.empty-search__word "xxxx"
      span.empty-search__subtitle Nenhum resultado foi encontrado para este termo. Por favor, realize uma nova busca.



block scripts
  //- tags javascript que serão inseridas no final do arquivo, antes do nosso JS principal
