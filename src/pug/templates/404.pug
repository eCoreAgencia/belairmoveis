extends ../layouts/default.pug

block vars
  - var bodyClass = 'error'
  - var pageTitle = '404'

block head
  //- conteúdo que será inserido dentro do `head` da página

block content
  .container
    .error__content
      h3.error__title OPS!
      span.error__subtitle A página que você procura não foi encontrada



block scripts
  //- tags javascript que serão inseridas no final do arquivo, antes do nosso JS principal
